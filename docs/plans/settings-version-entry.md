# 设置页版本入口：历史决策

2026-09-05 的入口整理，相关提交为 `50c5ec1` 与 `b2baa16`。当前产品契约见
[设置通用页与更新窗口](../../DESIGN.md)。

- 通用页仅显示运行中 App bundle 的版本和 build，以及打开更新窗口的入口。
- candidate、更新说明、下载与安装状态由更新窗口和菜单栏提示共同表达，设置页不复制。
- 当时曾在 build `1788111026` 的 AX 树观察到版本文本与按钮；这不是当前版本的运行证据。

原记录中的 Debug 共用正式更新通道问题属于旧身份方案；现行
[Debug / 正式身份隔离](../agent/development-builds.md) 已替代该前提。
