# XDial Next next-v0.9.0

## 更新了什么

- Profile 管理线路和规则；线路组与场景在整个配置库中共享，订阅刷新保留用户管理的场景和选择。
- 将配置、通用设置和 Profile 切换整合到同一设置窗口。
- 线路及线路组的长列表使用原生表格，减少滚动时重复构建详情和更新编辑状态的开销。
- 订阅条目的详情支持选择和复制，也可以复制成可编辑的本地线路或规则。
- 改善 Debug、Next、正式版并存时的后台服务识别，在替换服务前检查进程归属。

## 系统要求

- macOS 15 或更高版本，Apple Silicon Mac。
- XDial Next 使用独立应用、后台服务和网络扩展身份。
- 首次安装需要按 macOS 提示批准后台服务、网络扩展和所需权限。
- 新版共享 Profile 配置库，转换前保留旧配置；不要用不支持新版格式的旧版本改写共享库。
- 这是 Next 预发布通道，手动下载更新，不接收稳定版自动更新。
