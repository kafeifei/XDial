# XDial 设计规范

> 状态：Normative。本文约束 XDial 的视觉、信息层级和人机交互；网络与事务语义仍以
> [ARCHITECTURE.md](ARCHITECTURE.md) 为准。

本文首先约束 macOS 菜单栏 Popover、设置窗口和安装窗口。iOS / tvOS 可以按平台习惯
调整布局，但必须保留相同的状态含义、领域词汇与直接操作逻辑。

文中使用以下强度：

- **必须**：不可因局部实现方便而偏离；要改变时先更新本文并说明理由。
- **应该**：默认选择；只有明确的平台限制或更强的用户目标才可偏离。
- **可以**：允许的实现空间。
- **基线**：当前推荐 token 或尺寸；可以通过完整视觉验收后调整，不是架构常量。
- **实现漂移**：当前代码已知但不符合本文的地方；不得把它反写成设计标准。

---

## 1. 设计目标

XDial 不是仪表盘，也不是传统 VPN 的大开关。它是一个常驻菜单栏、按场景快速切换网络
裁决的工具。界面应同时满足六个目标：

1. **一眼可读**：打开 Popover 后，当前场景、连接状态、时长和速率无需展开即可确认。
2. **直接操作**：场景本身就是连接按钮；连接、取消、断开、切换和重试不另设文字按钮。
3. **忠于事实**：状态、步骤与错误只展示当前事务的结构化事实，不根据旧日志或界面外观猜测。
4. **容器从容、内容紧凑**：区域之间留足呼吸空间，同一信息块内部保持较小间距。
5. **低干扰**：不使用艳丽大色块、持续动画、重复标题、永久拖拽提示或无意义装饰。
6. **渐进披露**：高频信息始终可见；步骤、线路事实和配置字段按需展开。

一句话判断新设计是否合格：**用户能否在不读说明、不寻找按钮的情况下，理解当前发生了
什么，并完成下一步操作。**

### 1.1 领域词汇

| 中文 | 代码名 | 含义 |
|---|---|---|
| 线路 | `Line` | 一条可建立、复用并承载流量的出口 |
| 规则 | `RuleSet` | 一组匹配条件；可映射到线路 |
| 场景 | `Scenario` | 用户选择和连接的唯一入口；可由 SSID 自动选择 |
| 订阅 | `Subscription` | 产生或更新线路等资源的数据源 |
| 直连 | `Direct` | 不经过代理线路的明确出口 |

- **VOC-01（必须）**：用户界面与新代码统一使用“场景 / `Scenario`”，不得再用“模式”、
  “方案”、`Mode` 或 `Scheme` 指代同一领域对象。
- **VOC-02（必须）**：“反向”是 `RuleSet` 的匹配属性；SSID 是自动选择场景的条件。二者
  完全独立，不得共享文案、状态或数据结构。
- 用户输入的名称、SSID、URL、地址和原始错误保持原样，不翻译、不改写。

---

## 2. 产品表面与职责

| 表面 | 负责 | 不负责 |
|---|---|---|
| 菜单栏图标 | 提供唯一常驻入口与最小状态提示 | 展开系统级菜单、显示配置清单 |
| Popover | 场景连接/切换、连接时长与速率、必要运行事实、完整失败信息 | 编辑 Line / RuleSet / Scenario，显示永久操作栏 |
| 设置窗口 | 编辑 Line / RuleSet / Scenario 与通用选项 | 冒充运行时事实源、因展开卡片触发网络请求 |
| 安装与卸载窗口 | 展示安装事务、人工批准、卸载选项与可恢复错误 | 显示日常连接状态、在安装过程中创建网络配置 |

通用规则：

- 每个窗口只保留系统窗口已有的一份标题；内容区不得再硬加重复标题。
- 设置按钮只出现在 Popover 右上角，其他位置不得重复。
- 退出、安装状态等低频操作可以放在 Popover 的上下文菜单，不占主界面空间。
- 不创建全局浮层、系统级菜单或事件监听来演示设计；不得截获用户鼠标键盘。

---

## 3. 视觉语言

### 3.1 颜色

颜色表达状态，不表达装饰。产品配色采用低彩度的“雾感植物系”：烟紫、鼠尾草、燕麦与陶土
放在暖瓷白或炭灰表面上。它清新但不甜腻，明暗模式都不使用产品自定义蓝色。组件只消费
语义 token；固定颜色及 Light、Dark、Increase Contrast 变体集中在 `XDialPalette`，不得在
视图里散落近似色或直接使用 `.red`、`.green`、`.orange`、`.blue`、`Color.accentColor`。

| Token | Light | Dark | 用途 |
|---|---:|---:|---|
| Accent / Progress（烟紫） | `#756174` | `#BEA8BA` | 主要操作、连接进度、一般信息 |
| Selection（暖灰） | `#6F685F` | `#B8AFA4` | 当前 Tab、普通选中、反向开启；不表示成功 |
| Success（鼠尾草） | `#60745B` | `#94AA8E` | 已连接、已提交、成功 |
| Warning（燕麦） | `#91663A` | `#C69A65` | 外部批准、待应用、需要注意但尚未失败 |
| Danger（陶土） | `#A35145` | `#D18470` | 失败、破坏性操作、需要立即注意的错误 |
| Canvas（暖瓷） | `#F1EDE5` | `#1D1B19` | 设置画布与最底层背景 |
| Surface | `#F8F5EF` | `#25221F` | 普通卡片与表面 |
| Elevated | `#FEFBF6` | `#2F2B27` | Popover 与抬升表面 |
| Divider | `#D6CFC4` | `#49423B` | 分隔线与无状态细边框 |
| Text Primary | `#2B2723` | `#EEE8DF` | 正文与主要数值 |
| Text Secondary | `#6F685F` | `#B8AFA4` | 次要信息、空闲与禁用状态 |

使用约束：

- 大面积填充只用状态色的 `0.08–0.13` 透明度；边框通常为 `0.14–0.22`。
- 实色只用于细环、圆点、小徽标和图标，不铺满整张卡片。
- 等待外部批准使用 Warning；普通“处理中”使用 Progress，不把处理中误画成警告。
- 颜色必须同时配合图标、文字或轮廓；不得只靠红/绿区分。
- 产品自定义界面不得重新引入蓝色。系统键盘焦点环可以遵循用户的 macOS 强调色；不得为了
  “去蓝”隐藏、变淡或覆盖系统焦点反馈。
- 明暗模式均使用语义背景与正文色，不写死纯白或纯黑表面。
- High Contrast 下必须提高细边框、状态环与次要文字对比度；状态胶囊前景色必须按背景自适应，
  不得固定为白色。
- 语义层至少包含 `canvas`、`surface`、`elevated`、`divider`、`textPrimary`、
  `textSecondary`、`selection`、`focus`、`progress`、`success`、`warning`、`danger`、
  `disabled`。组件不得直接绑定原始 RGB。

### 3.2 间距

优先使用以下间距阶梯，不随手创造相邻数值：

| 等级 | 数值 | 用途 |
|---|---:|---|
| Micro | `4` | 同一字段内部、图标与短标签 |
| Compact | `6–8` | 信息行、卡片内部、胶囊内容 |
| Component | `10–12` | 同一组件中的分组 |
| Section | `14–16` | 卡片列表外边距、独立信息块 |
| Region | `18–24` | Popover 大区块与详情外边距 |

必须遵守“外松内紧”：列表画布水平边距不小于 `14`，详情区水平边距优先 `24`；同一信息块
内部行距保持 `4–8`。不得通过压缩外框留白来制造“紧凑”。

### 3.3 字体与数字

| 角色 | 规格 |
|---|---|
| 产品标题 | 正常文字尺寸下以 `15 pt / semibold` 为基线，可用 `0.25` tracking |
| 条目主标题 | 正常文字尺寸下以 `13 pt / medium` 为基线 |
| 正文与状态 | 正常文字尺寸下以 `12 pt / regular` 为基线，关键短状态可用 medium |
| 次要标签 | 系统 caption；正常文字尺寸下约 `11–11.5 pt` |

### 3.4 控件尺寸

- macOS 设置页中的行内布尔项必须使用紧凑开关，基线为 `.controlSize(.mini)`；不得用常规
  大号 Switch 撑高信息行。
- 缩小的是开关图形，不是文字与可操作区域：行高不得低于 `28 pt`，开关必须保留完整标签、
  键盘焦点与 VoiceOver 状态。
- 行内菜单和次要按钮以 `.controlSize(.small)` 为基线。场景圆球等主操作不属于行内设置控件，
  不得套用本条缩小。
| 微型状态 | 正常文字尺寸下约 `10–10.5 pt`，不得承载长句 |

- 时长、速率、IP 和计数使用等宽数字；速率值使用固定宽度，刷新时不得左右跳动。
- 长错误必须换行并可选择，禁止省略号截断关键原因。
- 不用缩小字体解决拥挤；先换行、增加行数或重新分组。
- 所有新用户文案必须提供中英文，术语固定为 Line / RuleSet / Scenario（线路 / 规则 / 场景）。
- 字体必须随系统文字尺寸缩放；在 `150%`、`200%` 与 `2×` 伪本地化下，关键内容仍须完整，
  窗口可以扩展或滚动，不能靠固定 frame 截掉内容。
- 用户文案进入统一的 String Catalog；不得散落硬编码中文、局部 `tr(zh,en)` 或通过字符串
  拼接生成复数和语法片段。数字、日期、单位与复数按当前 UI 语言和 locale 格式化。
- 普通名称尾部截断；路径与 URL 优先中间截断；SSID 按 §5.5 的单行胶囊规则从尾部截断。
  三者都必须提供完整可访问值；错误永不截断。

### 3.4 表面与形状

- 设置卡片圆角 `10`，默认边框 `0.5`、`Color.primary.opacity(0.065)`。
- 状态胶囊只用于短状态、速率、紧凑开关和步骤；不得把普通段落包成胶囊。
- 分隔线只分隔信息职责不同的区域，不给每一行画表格线。
- 场景圆球直径 `64`，内容槽宽 `72`；状态环永远在球体外缘。
- 图标优先使用 SF Symbols，线条保持 regular/medium；禁止拼出风格不明的混合图标。

---

## 4. Popover

### 4.1 尺寸与结构

Popover 从上到下只有以下区域：

1. 标题栏；
2. 场景区；
3. 仅在需要时出现的连接状态/进度区；
4. 已连接详情或失败详情。

尺寸合同：

- 最小宽度 `340`。
- 每行最多 `4` 个场景，超出后自动换行；每一行必须整体居中。最大宽度为四列的
  `378 pt`。
- 超过 `4` 个时，先取不超过四列的最少行数，再取能保持该行数的最少列数并均衡分行，
  使新增一行时 Popover 可以收窄。例如 `5 = 3 + 2`、`6 = 3 + 3`、
  `7–8 = 4 + 3/4`、`9 = 3 + 3 + 3`。禁止固定排成松散的 `4 + 1`。
- 场景槽宽 `72`、水平间距 `14`、区域水平边距 `24`、行距 `18`。
- 标题栏与速率栏高度均为 `54`。
- 场景数量变化时只做 `0.2s easeInOut` 的尺寸过渡，不横向滚动、不做 Cover Flow。
- Popover 高度不得超过当前屏幕可用区域；场景或详情过多时只允许整体纵向滚动，标题与主操作
  仍应优先保持可见。不得用无限增高把底部内容推到屏幕之外。

### 4.2 标题栏

- 左侧只显示 `XDial`、一个小状态点和连接时长。
- 连接时长只在标题栏显示，格式固定、使用等宽数字；其他区域不得重复。
- 设置齿轮永久固定在右上角，点击才打开设置窗口。
- 标题背景可以使用当前状态色的低透明度横向渐变，不使用亮色整块填充。

### 4.3 场景圆球

**POP-01（必须）**：圆球本身是唯一主操作，禁止在其下方新增“连接 / 断开 / 重试”文字
按钮。相同状态在鼠标、键盘和 VoiceOver 下必须执行同一个动作。

| 状态 | 外环 | 静态徽标 | 点击当前场景 | 点击其他场景 |
|---|---|---|---|---|
| 未选中 | 细灰色基础轮廓 | 无 | 连接 | 切换并连接 |
| 已选中未连接 | `3 pt` 灰环 | 无 | 连接 | 切换并连接 |
| 连接中 | `4 pt` 灰色虚线底环 + Progress 烟紫实际进度 | 无 | 取消本次连接 | 切换到目标场景 |
| 已连接 | `4 pt` Success 鼠尾草闭环 | 勾 | 断开 | 快速切换 |
| 失败 | `4 pt` Danger 陶土断环 | 叹号 | 重试 | 切换到目标场景 |

- 悬停只把下一步动作显示成右下角小徽标，不改变布局。
- 切换必须走保留当前连接的 Switch 事务，并复用连接池中身份相同、仍被目标场景需要的
  Line；这是运行语义，不在 UI 里增加说明。Switch 准备期间，当前场景继续保持 Success
  闭环与连接时长，目标场景显示 Progress 环；不得先把当前场景画成断开或清零时长。
- Switch 失败时，目标场景显示失败徽标和完整候选错误，当前场景仍保持 Success；文案必须
  明确“仍在使用〈当前场景〉”，不能把候选失败冒充成整机已经断线。只有旧运行 generation
  也失效时，才把当前场景转成失败态。
- 场景图标保存稳定的语义 Key，由各平台映射到统一线重的系统图标；配置不得保存 SF Symbol
  名。连接状态只改变环、徽标与颜色，不替换主图标。
- `nil` 表示自动：先匹配场景名称，名称无结果时才匹配该场景已经保存的 SSID。不得读取当前
  SSID、Line、RuleSet 或出口反推图标。手动选择永久优先，只有用户明确恢复自动才重新匹配。
- 未知 Key 或未识别语义使用稳定的网格 fallback；不得解码失败、覆盖未来 Key，或创造“像
  某物但说不清”的图标。
- 状态闭集还包括 `switching`、`reconnecting`、`disconnecting`、`rollingBack`、
  `rolledBack`、`disabled` 与 `configDirty`。它们应复用进行中、中性、警告或失败的语义
  token，并用状态文字/图标澄清；不得为每个状态再发明一种颜色。
- “已选中”只使用 Selection 暖灰；Success 只表示连接已经成功，Progress 只表示进行中或
  信息，Danger 只表示失败或危险。

### 4.4 连接进度

- 忙碌状态显示 `已完成/总数`，例如 `3/7`，整块可点击展开。
- 展开的每个步骤按内容宽度形成小胶囊，横向排布，空间不足自动换行。
- 胶囊受 Popover 内容宽度约束：短项按内容宽度排布，单项过长时独占一行并换行；失败步骤
  必须完整显示，不得溢出或省略关键内容。
- 首次连接步骤来自当前 `ConnectionReport.tasks`；Switch 步骤来自独立的候选事务报告，
  保持真实顺序与真实状态。候选报告不得覆盖仍在承载流量的 committed 连接报告。
- 等待、进行中、完成、失败、回滚必须同时有图标和文字，不能只变色。
- 连接步骤与连接成功后的 Line / RuleSet 详情共用同一个展开状态和同一个 disclosure 图标；
  `chevron.right` 表示已收起，`chevron.down` 表示已展开。连接阶段变化和 transaction 更新不得
  重置用户选择或切换成另一套箭头语义。
- 展开/收起使用 `0.18s easeInOut`，不得使用无限旋转装饰动画。

### 4.5 速率栏

- 下载与上传组成一个居中的紧凑胶囊；右侧只保留详情展开箭头。
- 两个方向之间使用短 Divider 和固定间距，整体不随数字宽度移动。
- 每个值槽宽 `70`，字体 `11 pt monospaced`，自动选择 `B/s`、`KB/s`、`MB/s`、`GB/s`。
- 始终保留一位小数，例如 `0.0 B/s`、`14.1 KB/s`。
- 首次详情默认展开；此后沿用连接步骤的共同展开状态。箭头只收起步骤、线路与规则事实，不
  收缩 Popover 外框。

### 4.6 已连接详情

已连接详情分成两个明确区域，并用一条弱 Divider 分开：

1. 本次事务实际使用的 Line 与观察到的地址；
2. 当前 Scenario 中完整的 `RuleSet → Line` 清单。

规则：

- 区域外边距使用 `24 × 16`，信息行内部垂直间距 `6–8`。
- Line 名称与地址分列，地址使用等宽字体；无本次事务事实时不伪造“运行中”。
- 规则清单优先两列排布，条目过长时换行，箭头仅表达映射关系。
- 不显示“策略”之类冗余区块标题；内容本身足以说明时不再加标题。
- 最后一条横向状态区默认不存在；仅在当前报告确有错误时显示完整错误。

### 4.7 失败详情

- **ERR-01（必须）**：第一块给出准确的用户可读结论；标题由结构化 error code、task/resource
  和 layer 生成本地化摘要，能归因到任务时必须包含任务名称。
- 第二层保留完整原始错误，允许换行和文本选择，禁止 `...`。
- 同时显示系统接管是否提交、回滚是否完成、原网络是否可用。
- 错误来源必须是当前 `ConnectionReport`；不得解析日志来生成 UI 状态。
- 不得通过搜索 `SERVFAIL`、`timed out`、`certificate` 等错误字符串猜类别或替换摘要；原始
  message 只能作为详情原样展示。回滚和“已移除系统接管”是独立事实，不得覆盖原始故障。
- 失败色只强调错误图标、细环和重点文字，不把整个 Popover 涂红。

### 4.8 当前连接待应用配置

- 仅当已保存配置的**有效运行指纹**与当前事务记录的指纹不同时显示；不得用整份 Profile
  是否变化代替该判断。
- 提示位于场景区之后、速率与详情之前，使用 `40–44 pt` 的 Warning 状态轨；它不是
  错误，不改变已连接场景的绿色闭环，也不藏进可折叠详情。
- 文案使用“修改已保存，当前连接尚未应用”，不得声称只有 Scenario 发生变化。
- 整条状态轨可执行“应用并重连”，右侧使用 `arrow.clockwise`；Popover 不再增加独立的大型
  文字按钮。触发后原位进入正常连接进度状态。
- 优先级固定为：完整失败信息 > 连接/切换进度 > 待应用配置 > 普通已连接详情。

---

## 5. 设置窗口

### 5.1 框架

- 默认内容尺寸 `540 × 520`。
- 窗口标题已经存在，内容区不得新增第二个 `XDial 设置`。
- 顶部 Tab 区高 `54`；线路、规则、场景组成一个分组，在左侧可用区域内相对居中；通用设置
  使用独立胶囊固定在最右侧。不得恢复成四项等宽连成一个分组。
- 当前 Tab 使用 Selection 暖灰的低透明度胶囊；未选项使用 Text Secondary。
- 内容画布使用轻微灰层，卡片使用系统窗口背景，形成“画布—卡片”两级即可。
- 通用页与其他设置共用字体层级：控件正文 `12 pt`、说明 `10.5–11 pt`、小节标题
  `12 pt / semibold`；不得退回系统默认大号正文造成单个 Tab 突兀。
- 通用页外观选择使用紧凑的三段图标：半明半暗表示跟随系统、太阳表示白天、月亮表示黑夜；
  图标必须有完整的帮助与辅助功能名称，不得只靠外形让用户猜。选择只改变 XDial 的界面
  配色并单独持久化，不进入 Profile、不显示待重连提示，也不触发网络动作。

### 5.2 卡片列表

- 列表水平边距 `14`、垂直边距 `12`、卡片间距 `8`。
- 卡片内部水平边距 `11`、垂直边距 `9`、内部间距 `7`。
- Line、Subscription、RuleSet、Scenario 默认全部折叠。
- 单击标题行展开；控件、删除和开关必须消费自己的点击，不得误触展开。
- **SET-01（必须）**：用户可见的排序入口只有“按住整条约 `300 ms` 后拖动”；不得增加永久
  拖拽把手、排序按钮或说明文案。短按仍然只负责展开/收起。
- 拖动只能从标题行的非控件空白区域开始；TextField、Picker、Toggle、删除、刷新、SSID、
  行内动作及展开详情中的长按不得启动拖动。
- Line、Subscription、RuleSet、Scenario 分别只在同类集合中排序；Scenario 内的 RuleSet
  绑定只在当前 Scenario 内排序。不得跨类型或跨 Scenario 放置。
- 达到长按阈值后卡片临时抬起，拖动中显示清晰的插入缝/目标态，并支持滚动区域边缘自动滚动。
  `Escape`、拖出有效区域或落入错误类型时取消且不保存；只有成功 drop 后才持久化。
- 拖动结束不得顺带触发展开、开关、删除或激活。
- VoiceOver / 键盘必须提供不占视觉空间的“上移 / 下移”可访问性动作，不能让拖动成为
  唯一可达路径。

### 5.3 卡片标题

- 顺序固定为：展开状态、运行事实或活动状态、名称、必要摘要、类型/紧凑操作、启用、删除。
- 运行状态来自当前事务；“对象已启用”不能显示成“正在运行”。
- 活动 Scenario 使用 Selection 暖灰的勾与左侧细强调条；只有正在运行成功的状态才使用
  Success，不使用整块高饱和背景。
- 摘要必须一行可扫读；长地址可中间截断，但错误不得截断。
- 删除始终位于最右侧，使用系统 trash；破坏性确认遵循平台习惯。

### 5.4 RuleSet

- “反向”是 RuleSet 的匹配属性，URL、手动和应用规则均支持。
- 反向开关位于折叠标题行，以紧凑胶囊表达；开启用 Selection 暖灰，关闭为中性。
- 不在详情里重复一整行“反向匹配（匹配列表之外）”。
- URL 后紧跟获取 Line 选择器，默认 Direct；格式等低频字段放下一行。
- 编辑器内部紧凑，但域名、CIDR、程序规则必须有清晰分组。
- “反向”必须覆盖所有支持匹配语义的 RuleSet 类型，不得只在某一种来源上实现。

### 5.5 Scenario 与 SSID

- 折叠标题在活动状态之后、名称之前保留固定 `18–20 pt` 图标槽；状态标记与场景图标是两个
  独立语义，不能互相替代。
- 展开后把图标入口收在名称行右侧的 `28–32 pt` 按钮中；点击显示限宽六列网格。预设以家庭、
  公司、酒店、旅行、公共 Wi-Fi、校园、影音、开发、隐私、全局、直连等语义为基线，不永久
  摊开网格，也不显示 SF Symbol 技术名。
- 选择器首项为“自动匹配”，同时预览当前推导结果；手选立即保存并关闭。只改图标属于纯展示
  变化，不得改变运行指纹、显示待重连提示或发起网络操作。
- 折叠标题中的场景名称、规则摘要与全部已绑定 SSID 必须位于同一行；每个 SSID 显示为独立
  胶囊，不得另起一行或增加卡片高度。空间不足时先压缩较宽胶囊，每个胶囊最低保留约两个
  中文字符的文字宽度，再从尾部省略；完整 SSID 仍通过 help 与可访问值提供。达到全部胶囊
  的最小宽度后，超出标题可用区域的尾部内容自然裁切，不得反向挤压场景名称、状态与右侧控件。
- 一个 SSID 只能绑定一个 Scenario；冲突错误紧邻输入框，明确指出当前归属。
- 展开后显示当前 Wi-Fi、已绑定列表、添加入口以及完整 `RuleSet → Line` 顺序。
- 初始化读取和用户点击“读取当前 Wi-Fi”时均应触发位置权限流程；拒绝后提供打开系统设置
  的显式入口。
- SSID 只负责选择 Scenario，不进入 sing-box 数据面，也不表达“可信网络”。

### 5.6 配置生效

- 编辑立即保存，但不得谎称已作用于当前连接。
- 只有会改变当前事务有效运行闭包的保存差异才提示：当前 Scenario 的有效 binding 顺序与
  默认目标、实际使用的 Line / RuleSet / Subscription、URL RuleSet 的 fetch Line，以及
  它们真正参与生成的字段。
- 未使用对象、顶层视觉排序、显示名称、Scenario SSID、`verified`、订阅 `updatedAt`、
  语言、登录启动和自动连接等变化不得提示。失效配置与当前事务指纹不同，也必须提示，
  由重新规划给出具体错误。
- 指纹由 Go 控制面与 `ConnectionPlan` 同源生成并写入 `ConnectionReport`；Swift UI 只比较
  不透明指纹，不得复制依赖筛选或生成规则。
- 四个 Tab 共用一条位于 Tab 行下方的 `34–36 pt` Warning 状态轨，无独立圆角大卡片
  和阴影。文案为“修改已保存，当前连接尚未应用”，右侧提供轻量“应用并重连”。
- 只有用户明确点击重连或切换场景才应用变化；展开卡片、切换 Tab、排序本身不得发起网络请求。

---

## 6. 安装与卸载窗口

- 安装与卸载复用同一个窗口；通用页只显示“安装 / 卸载”两个入口，不复制确认、进度或错误 UI。
- 顶部在同一标题区内保留紧凑的“安装 / 卸载”切换，并只显示一个状态图标、一个标题和一句说明。
- 安装页按事务顺序列出任务；任务行包含状态图标、名称、详情和必要操作。
- 卸载页集中选择是否同时删除线路、规则、场景和钥匙串密码；破坏性动作必须二次确认。
- 安装与卸载事务互斥；安装尚在进行时可以查看卸载页，但不得同时启动卸载。
- 卸载执行中禁止重复操作，并在原窗口显示进度；失败详情完整换行且可重试，不返回通用页另行报错。
- 安装页底部固定说明“安装过程不会创建网络配置或接管流量”；卸载页明确说明会先断开并移除系统接管。
- 安装就绪状态的“完成”是默认动作；Return 与 Escape 都应关闭就绪窗口。卸载不得绑定 Return，避免误触不可逆操作。
- 进行中、待批准、成功、失败沿用统一的状态色语义，不另造一套高饱和配色。

---

## 7. 输入、动效与焦点

### 7.1 鼠标与触控板

- 悬停反馈不得改变组件尺寸或导致文本位移。
- 滚轮与触控板使用系统 ScrollView 行为，不捕获全局滚动事件。
- 圆球和卡片标题行都应有连续命中区；纯图标控件最小命中区为 `28 × 28`，视觉图形可以更小。
- 长按拖动与单击展开必须可区分；拖动结束不得顺带触发点击。
- Hover 只增强已有信息，不能成为唯一状态或唯一入口；鼠标移入不得自动切换场景或焦点。

### 7.2 键盘与辅助功能

- Popover 中 `Escape` 关闭；方向键在场景网格移动焦点，`Space` / `Return` 执行圆球当前动作，
  `Command-,` 打开设置，`Tab` 进入进度与详情控制。
- 设置中 `Tab` / `Shift-Tab` 全覆盖；左右键切换 Tab，并在折叠卡片上执行收起/展开；
  `Command-W` 与 `Escape` 遵循窗口或 Sheet 的系统语义。文本输入中 `Return` 提交，`Escape`
  取消尚未提交的编辑。
- 焦点顺序遵循视觉顺序，焦点环始终可见；状态刷新、自动保存与排序不得抢走焦点。
- 纯图标按钮必须有 AX label；有状态的控件还必须提供 AX value 与必要 hint，不能只依赖
  `.help`。折叠卡片必须暴露为真正的 disclosure/button，并报告 expanded/collapsed。
- 状态圆球必须读出“场景名、当前状态、当前可执行动作”；连接中再读出 `3/7` 等进度。
- 连接状态不能只靠颜色；VoiceOver 文案必须说清“连接中 / 已连接 / 失败”等状态。
- 错误文本可聚焦、可选择；长文本换行而不是缩到不可读。
- 中英文、浅色/深色、Increase Contrast、Differentiate Without Color、Reduce Motion 和
  VoiceOver 是同一组件的必测变体。

### 7.3 动效

| 场景 | 时长 |
|---|---:|
| 展开、收起、详情切换 | `0.18–0.20s easeInOut` |
| 连接进度变化 | `0.25s easeInOut` |
| Popover 随场景数量调整 | `0.20s easeInOut` |

- 动效只解释状态变化，不作为装饰。
- Reduce Motion 下取消几何位移和非必要过渡，保留立即可读的最终状态。
- 不使用持续弹跳、发光、强呼吸或大面积颜色闪烁。

---

## 8. 文案与错误

- 操作能由图形和直接操控表达时不加文字按钮；配置字段和不可逆操作必须明确命名。
- 用户可读错误采用“对象/步骤 + 发生了什么”，不要把低层英文错误当标题。
- 原始错误作为第二层完整保留，不能用一个泛化文案覆盖关键事实。
- 不使用“策略”指代 RuleSet 或 Scenario；界面统一使用“规则”和“场景”。
- 不在不同区域重复同一状态。连接时长只在标题；速率只在速率栏；设置只在右上角。
- 运行事实、用户可读摘要和原始错误是三层不同内容；不得用其中一层替代另外两层。

---

## 9. 禁止事项

- 不增加独立的连接、断开、重试文字按钮来绕过场景圆球。
- 不使用横向滚动、Cover Flow、3D 透视或隐藏滚动条承载场景。
- 不把外框压紧、内容摊开；必须保持“外松内紧”。
- 不为每个区域加标题、边框和背景，形成层层套盒。
- 不截断关键错误，不把日志猜测包装成准确结论。
- 不因对象 `enabled` 就显示绿色运行态。
- 不在 Popover 其他位置复制设置入口或连接时长。
- 不加入永久排序图标、拖动教学或大段帮助文案。
- 不打开会抢占系统输入的全局菜单、预览器或导出界面来验证 UI。

---

## 10. 当前实现的解释边界

当前截图和代码不是设计标准的反向来源。以下已出现过的形态一律视为实现漂移，不得据此修改
本文或在新组件中复制：

- 通过错误字符串子串猜摘要，而不是消费结构化错误字段；
- Subscription 使用与其他折叠卡片不同的圆角、padding 或强调色；
- 直接使用系统红/绿/橙/accent，绕过语义 palette；
- 用场景名称关键词猜图标，而不是保存稳定的图标语义；
- 把 `.draggable` 覆盖到整张卡片及行内控件，且没有长按阈值、插入态、边缘滚动和取消语义；
- 折叠头只用 `onTapGesture`，没有 disclosure 角色、键盘焦点或完整 AX 状态；
- 设置表单列宽、缩进或卡片样式各自为政，以及安装页或设置页散落硬编码中文；
- 固定字号和固定 frame 导致大文字、长文案或伪本地化被截断。

修复这些偏差时应以本文合同为准，不应为了保留旧截图而增加例外。

---

## 11. 设计变更与验收

任何 UI 改动在合入前必须回答：

1. 它属于哪个产品表面，是否越过该表面的职责？
2. 它使用了现有 token 和间距阶梯吗？若新增 token，为什么现有值不能表达？
3. 空闲、连接中、成功、失败、禁用和配置已变化分别是什么样？
4. 中文、英文、伪本地化、浅色、深色、高对比和长错误是否仍可读？
5. 鼠标、触控板、键盘和 VoiceOver 是否都能完成操作？
6. 是否引入布局跳动、重复信息、永久提示或额外网络副作用？
7. 运行事实是否仍来自当前结构化报告？

最小视觉验收矩阵：

| 表面 | 必测状态 |
|---|---|
| Popover | `0 / 1 / 5 / 6 / 10 / 50` 个场景；idle、selected、connecting `0/7、3/7、7/7`、switching、connected、failed、rolling back、dirty；超长名称和完整长错误 |
| 设置 | 四个 Tab、全部折叠、单卡展开、长名称/SSID/URL、SSID 权限三态与冲突、长按阈值、有效/无效 drop、取消、边缘滚动、重开后顺序 |
| 安装 | 检查中、待系统批准、就绪、可重试失败、不可重试失败 |

上述每项还必须覆盖：

- Light / Dark × Normal / Increase Contrast；Reduce Motion 与 Differentiate Without Color；
- 中文、英文、`2×` 伪本地化；`100% / 150% / 200%` 文字尺寸；
- 鼠标、触控板、键盘 only 与 VoiceOver；
- 速率 `0`、`999.9`、跨 `KB / MB / GB` 边界，布局始终不跳；
- 短中文、长中文、长英文及底层原始错误，全部可换行、选择和复制。

Debug AX 可以验证结构，但不能替代真实键盘焦点、VoiceOver 朗读、长按拖放、触控板和系统
权限联调。

实现验证仍遵守 [AGENTS.md](AGENTS.md)：先编译成功，再让旧进程正常退出并启动新构建；
通过 Debug Server 检查真实窗口与 AX 树。需要鼠标、触控板或系统授权才能确认的行为，交付
可运行版本后由用户现场验证，不得为自动验收而抢占用户输入。
