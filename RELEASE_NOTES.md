# XDial v0.8.3

## 更新了什么

- 菜单栏图标意外消失时自动检测并尝试重建，避免菜单项被移除时连带退出应用、中断连接。
- 为菜单恢复增加等待与重试上限，避免全屏、隐藏菜单栏和休眠期间误判；保留用户主动退出与系统退出的正常流程。
- 针对 macOS 26 的错误菜单归属，在权限允许且用户仍启用 XDial 图标时，备份并定点修复关联；权限不足时保留连接，不反复重试。
- 等待首次 Wi-Fi 名称授权检查完成后再显示设置提示，减少启动时的误提示。
- 网络代理切换后重新建立系统 DNS 查询，改善切换后的域名解析恢复。

## 系统要求

- macOS 15 或更高版本。
- 首次安装时需要按系统提示批准 XDial 的后台服务和网络扩展。
