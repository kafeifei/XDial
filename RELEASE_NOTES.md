# XDial v0.7.4

## 更新了什么

- 线路 IP 区域支持选择 IPv4 / IPv6，地址右对齐，并显示查询进度、失败和重试状态。
- 修复下载启动时应用重定位可能导致重复安装、等待旧实例退出的问题，保留安装失败时的回滚能力。
- 完善升级时 helper 的注销、重新注册和运行版本核验，对已确认的特定注册失败执行一次受限恢复。
- 保留 0.7.3 的 Wi-Fi 场景选择、自动重连准备和并发恢复修复。

## 系统要求

- macOS 15 或更高版本。
- 需要在系统提示时批准 XDial 的 System Extension。
