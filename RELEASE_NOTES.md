# XDial v0.8.0

## 更新了什么

- 完善完整安装、升级与降级流程，同步替换并核验应用、后台服务和系统扩展，修复重复安装及等待旧实例退出的问题，并保留安装失败时的回滚能力。
- 修复后台服务注销、重新注册和运行版本核验问题；卸载等待后台进程退出，临时产物清理仅处理 XDial 自有文件。
- 改善受限网络中的 Direct 连接准备，避免单个固定外部目标访问失败阻止整个场景连接；线路重新验证成功后清除该线路的旧失败。
- 完善线路独立恢复和场景切换，自动切换遇到瞬态错误时保留目标并限次重试，休眠后继续未完成的切换。
- DNS UDP 查询保留原始解析器端点，修复转发过程中的目标信息丢失。
- 线路 IP 区域支持选择 IPv4 / IPv6，显示查询进度、失败和重试状态；IP 地址菜单展示完整地址。

## 系统要求

- macOS 15 或更高版本。
- 需要在系统提示时批准 XDial 的 System Extension。
