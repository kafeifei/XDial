# XDial v0.8.1

## 更新了什么

- 修复覆盖升级和降级时后台服务注册可能报“Operation not permitted”的问题，在系统注册状态尚未同步时自动进行有界重试。
- 支持直接接手旧版本安装失败留下的维护状态，无需先卸载再重新安装。
- 后台服务更新仍核对实际运行版本和本地控制通道，安装失败保留可恢复状态。
- 开发版与正式版使用独立的组件身份、配置和维护记录，便于保持正式使用与开发验证互不干扰。

## 已合入 main，待后续版本发布

- 保留完整安装、卸载及失败回滚流程，并整合后台服务注册状态的有界重试。
- 保留线路独立恢复、受限网络就绪检查、场景切换重试和休眠恢复。
- DNS UDP 保留原始解析器端点；UDP 流跨引擎切换继续工作，首批数据等待关联就绪，连续握手失败受重试预算限制。
- 更新元数据改为 GitHub Pages 清单，下载和安装前重新确认版本、完整摘要及通道身份。
- macOS helper 显式保持 15.0 最低系统版本，发布和开发包按每个架构核验实际二进制。
- IP 地址菜单保留完整地址、地址族选择及查询重试状态。

## 系统要求

- macOS 15 或更高版本。
- 首次安装时需要按系统提示批准 XDial 的后台服务和网络扩展。
