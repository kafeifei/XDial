# XDial v0.7.3

## 更新了什么

- “通用”设置现在直接显示当前版本与构建号，并提供“检查更新”入口，可随时打开现有更新窗口。
- 修复网络环境变化后 Transparent Proxy 会话丢失时，自动重连可能沿用旧场景配置或旧 Underlay 的问题；重连现在会等待网络稳定，并使用最新选择的场景重新生成配置。
- 修复在同一 Wi-Fi 下手动切换场景后，DNS 或默认路由变化触发的网络刷新可能把场景切回 SSID 自动匹配结果的问题。
- 收紧自动重连与场景切换的并发边界，避免失效 Provider 的迟到回调覆盖当前恢复目标或重复发起恢复事务。

## 系统要求

- macOS 15 或更高版本。
- 需要在系统提示时批准 XDial 的 System Extension。
