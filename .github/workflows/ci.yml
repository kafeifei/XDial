name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  go:
    name: Go (lint + vet + test)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true
      - name: gofmt gate
        run: |
          unformatted=$(gofmt -l core/ cmd/)
          if [ -n "$unformatted" ]; then
            echo "以下文件未 gofmt:" && echo "$unformatted" && exit 1
          fi
      - name: prepare patched Go dependencies
        run: make prepare-patched-go
      - name: go vet
        run: make go-vet
      - name: go build
        run: make go-build
      - name: install sing-box test validator
        run: |
          make sing-box-test-validator
          echo "$PWD/build/tools" >> "$GITHUB_PATH"
      - name: go test
        run: make test

  macos:
    name: macOS (swift build + bundle)
    runs-on: macos-15
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true
      - name: make app (debug)
        run: make app
      - name: connection transaction tests
        run: make test-macos-transaction
      - name: make release
        run: make release
